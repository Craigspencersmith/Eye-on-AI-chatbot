services:
  chatbot:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./credentials:/app/credentials:ro
      - chroma_data:/app/chroma_data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  chroma_data:
